services:
  - type: web
    name: ct-investments
    runtime: python
    buildCommand: echo "No build needed"
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
